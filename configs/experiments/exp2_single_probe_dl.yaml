_base_: ../default.yaml

experiment_name: exp2_single_probe_dl

model:
  type: cnn1d
  config: configs/models/cnn1d.yaml

training:
  target: density
  device: auto
  max_epochs: 200
  batch_size: 128
  early_stopping_patience: 20
