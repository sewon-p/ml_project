_base_: ../default.yaml

model:
  type: lstm
  config: configs/models/lstm.yaml

training:
  target: density
  device: cuda
  max_epochs: 200
  batch_size: 128
  num_workers: 0
  early_stopping_patience: 20

output_dir: outputs_lstm/
