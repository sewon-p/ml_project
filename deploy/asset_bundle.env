# Deployment asset bundle configuration.
# Code releases and deployable assets are intentionally decoupled.
#
# Update ASSET_RELEASE_TAG only when the deployed model/GIS bundle changes.
# Normal code-only releases should not require asset re-upload.

ASSET_RELEASE_TAG=v0.3.0
MODEL_ASSET=xgboost_best.pkl
GIS_ASSET=seoul_links.geojson
